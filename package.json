{
  "name": "propdesk-myfxbook-proxy",
  "version": "1.0.0",
  "description": "Standalone proxy for Myfxbook API calls, avoiding Cloudflare Worker IP blocking",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
